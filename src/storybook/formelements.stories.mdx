import CNumericInput from '../components/NumericInput/NumericInput.vue';
import { Meta, Canvas, Story } from '@storybook/addon-docs';

<Meta
  title="Concrete/Form Elements"
  component={CNumericInput}
/>

# Form Elements

All form inputs have the ability to wrap them selves in a form element component. 
By default form inputs will use the `wrapFormInputs` property in the concrete options unless the `noWrap` property has been set on the form input.

```javascript dark
// entry.js
app.use(Concrete, {
  wrapFormInputs: true // default value
});
```

## Examples

#### Basic
<Canvas withSource='open'>
  <Story name="Basic">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput />`,
    })}
  </Story>
</Canvas>

#### No Wrap
<Canvas withSource='open'>
  <Story name="No Wrap">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput no-wrap />`,
    })}
  </Story>
</Canvas>

#### Label
<Canvas withSource='open'>
  <Story name="Label">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput label="Test Label" />`,
    })}
  </Story>
</Canvas>

#### Label and No Wrap
<Canvas withSource='open'>
  <Story name="No Wrap and Label">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput label="Test Label" no-wrap />`,
    })}
  </Story>
</Canvas>

#### Label and No Label
<Canvas withSource='open'>
  <Story name="No Label and Label">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput label="Test Label" no-label />`,
    })}
  </Story>
</Canvas>


#### ID
<Canvas withSource='open'>
  <Story name="ID">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput id="firstname" />`,
    })}
  </Story>
</Canvas>

#### ID and No Wrap
<Canvas withSource='open'>
  <Story name="ID and No Wrap">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput id="firstname" no-wrap />`,
    })}
  </Story>
</Canvas>

#### ID and No Label
<Canvas withSource='open'>
  <Story name="ID and No Label">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput id="firstname" no-label />`,
    })}
  </Story>
</Canvas>

#### ID and Label
<Canvas withSource='open'>
  <Story name="ID and Label">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput id="firstname" label="First Name" />`,
    })}
  </Story>
</Canvas>

#### ID and Label and Label Formatter
<Canvas withSource='open'>
  <Story name="ID and Label and Label Formatter">
    {(args) => ({
      components: { CNumericInput },
      setup() { return { args }; },
      template: `<CNumericInput id="firstname" label="First Name" :labelFormatter="(props) => props.id.toUpperCase()" />`,
    })}
  </Story>
</Canvas>