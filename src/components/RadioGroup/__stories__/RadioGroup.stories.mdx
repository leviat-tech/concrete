import { ArgsTable, Story, Canvas, Meta, Source  } from "@storybook/addon-docs";
import CRadioGroup from '../RadioGroup.vue';

<Meta
  title="Components/RadioGroup"
  component={CRadioGroup}
  argTypes={{
    size: {
      options: ['lg', 'md', 'sm', 'xs'],
      control: { type: 'select' },
    },
    color: {
      options: ['default', 'indigo', 'sky', 'steel', 'success', 'warning', 'danger'],
      control: { type: 'select' },
    },
    disabled: {
      control:'boolean',
    },
    columns: {
      control:'number',
    }
  }}
/>

<!--- Component template -->

export const componentTemplate = (args) => ({
  components: { CRadioGroup },
  setup() {
    return { args };
  },
  data() {
    return {
      value: null,
    }
  },
  template: '<CRadioGroup class="w-64" id="RadioGroup" v-bind="args" v-model="value"></CRadioGroup>',
});

# RadioGroup

<Canvas withSource='open'>
  <Story name="Overview" height="200px"
        args={{
          id: 'RadioGroup',
          options: [ 'Pugh', 'Barney', 'McGrew', 'Cuthbert', 'Diddle', 'Gruff' ],
          onChange: null,
          colour: 'sky',
          formatter: (l) => l.toUpperCase()
        }}>
    {componentTemplate.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable story="Overview" />

## Columns