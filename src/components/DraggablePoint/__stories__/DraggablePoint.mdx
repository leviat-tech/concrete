import { Controls, Story, Canvas, Meta, Source } from '@storybook/blocks';
import CDraggablePoint from '../DraggablePoint.vue';
import * as DraggablePointStories from './DraggablePoint.stories';

<Meta of={DraggablePointStories} />

export const componentTemplate = (args) => ({
  components: { CDraggablePoint },
  setup() {
    return { args };
  },
  data() {
    return {
      point: { x: 50, y: 50 },
    };
  },
  methods: {},
  template: `<svg class="bg-gray-200" viewBox="0 0 100 100" width="200" height="200">
  <CDraggablePoint
    :point="point"
    @dragging="(pt) => point = pt"
    @drag-end="(pt) => point = pt"
    v-bind="args"
  />
</svg>`,
});

# Draggable Point

<Canvas transformSnippet="open">
  <Story of={DraggablePointStories.Overview} />
</Canvas>

## Props

<Controls />
