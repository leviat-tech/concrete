import { ArgsTable, Story, Canvas, Meta, Source  } from "@storybook/addon-docs";
import CAutocomplete from '../AutoComplete.vue';

<Meta
  title="Components/AutoComplete"
  component={CAutocomplete}
  argTypes={{
    size: {
      options: ['lg', 'md', 'sm', 'xs'],
      control: { type: 'select' },
    },
    color: {
      options: ['default', 'indigo', 'sky', 'steel', 'success', 'warning', 'danger'],
      control: { type: 'select' },
    },
    placeholder: {
      control:'text',
    },
    disabled: {
      control:'boolean',
    }
  }}
/>

<!--- Component template -->

export const componentTemplate = (args) => ({
  components: { CAutocomplete },
  setup() {
    return { args };
  },
  data() {
    return {
      value: null,
    }
  },
  template: '<CAutocomplete class="w-64" id="Autocomplete" v-bind="args" v-model="value"></CAutocomplete>',
});

# AutoComplete

<Canvas withSource='open'>
  <Story name="Overview" height="200px"
        args={{
          id: 'Autocomplete',
          options: [ 'Pugh', 'Barney', 'McGrew', 'Cuthbert', 'Diddle', 'Gruff' ],
          onChange: null,
        }}>
    {componentTemplate.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable story="Overview" />


## With Formatter

<Canvas withSource='open'>
  <Story name="Formatter" height="200px"
        args={{
          id: 'FormattedAutocomplete',
          options: [ '10', '20', '30', '40', '50', '60', '70', '32' , '33' , '36', '45', '55' ],
          onChange: null,
          formatter: (l) => l + String.fromCharCode(32, 99, 109) ,
        }}>
    {componentTemplate.bind({})}
  </Story>
</Canvas>
