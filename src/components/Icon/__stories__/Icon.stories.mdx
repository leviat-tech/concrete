import { ArgsTable, Story, Canvas, Meta, Source } from "@storybook/addon-docs";
import CIcon, { icons } from '../Icon.vue';


<Meta
  title="Components/Icon"
  component={CIcon}
  argTypes={{
    size: {
      options: ['sm', 'md', 'lg', 'xl', '2xl', '3xl'],
      control: { type: 'select' },
    },
    type: {
      options: Object.keys(icons),
      control: { type: 'select' },
    },
    color: {
      options: ['default', 'indigo', 'sky', 'steel', 'success', 'warning', 'danger'],
      control: { type: 'select' },
    },
    spin: {
      control: 'boolean'
    }
  }}
/>

<!--- Component template -->

export const allIconsTemplate = (args) => ({
  components: { CIcon },
  setup() {
    return { args, icons };
  },
  template: `
<div class="flex flex-wrap">
  <div v-for="icon, type in icons" class="float-left text-center mb-10">
    <div class="flex justify-center items-center m-1 border rounded w-24 h-24">
      <CIcon :type="type" />
    </div>
    <div class="text-xs">{{ type }}</div>
  </div>
</div>`,
});

export const interactiveIconTemplate = (args) => ({
  components: { CIcon },
  setup() {
    return { args, icons };
  },
  template: `<CIcon v-bind="args" />`
});

<!--- Component documentation -->

# Icon
<Canvas withSource="open">
  <Story name="Overview"
        args={{
          size: 'md',
          type: 'hand',
          spin: false
        }}>
    {interactiveIconTemplate.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable story="Overview" />

## Icons Reference

<Canvas>
  <Story name="Icons Reference">
    {allIconsTemplate.bind({})}
  </Story>
</Canvas>